# 📚 Books API

API REST básica desarrollada con **Node.js, Express y TypeScript** como ejercicio integrador del Módulo 3.

---

## 🚀 Instalación y ejecución

```bash
npm install
npm run dev
```

Servidor disponible en:

```
http://localhost:3000
```

---

## 🔐 Autenticación

Enviar el siguiente header en Postman:

```
Authorization: 123456
```

---

## 📌 Endpoints

* `GET /books` → Lista todos los libros
* `GET /books?author=NombreAutor` → Filtra libros por autor
* `GET /books/:id` → Obtiene un libro por ID
* `POST /books` → Crea un libro
* `PUT /books/:id` → Actualiza un libro
* `DELETE /books/:id` → Elimina un libro

---

## 🧠 Tecnologías

* Node.js
* Express
* TypeScript
* cors

---

👩‍💻 **Veronica Muzzio**
